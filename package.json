{
  "name": "monorepo-chatapp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn workspace server start:prod",
    "build": "yarn server:build && yarn front:build && yarn front:generate",
    "dev": "yarn front:dev & yarn server:dev",
    "front:dev": "yarn workspace front dev",
    "front:build": "yarn front:gql && yarn workspace front build",
    "front:gql": "yarn workspace front graphql:codegen",
    "front:generate": "yarn front:gql && yarn workspace front generate",
    "server:dev": "yarn workspace server start:dev",
    "server:build": "yarn workspace server build"
  },
  "workspaces": [
    "./apps/*"
  ]
}
